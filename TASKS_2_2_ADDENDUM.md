# Version 2.2 Task Addendum Integration Notice

The `U22-*` tasks are now merged into the canonical `TASKS.md` dependency graph. This file remains only as update history. Do not duplicate the tasks or create a second completion state.
