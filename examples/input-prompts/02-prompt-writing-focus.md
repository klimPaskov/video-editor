# Prompt: focus and prompt-writing pacing

Apply this approved editorial policy to `<project-id>`:

- Remove these exact spoken phrases: `<quote 1>`, `<quote 2>`.
- Remove the sentence beginning: `<opening words of sentence>` while the
  visible target is `<page/window description>`.
- Cut from `<exact phrase before cut>` directly to `<exact phrase after cut>`.
- Remove thinking pauses, dead air, and low-confidence `uh`/`um` fillers only
  when the join preserves the word, meaning, cadence, and room tone.
- Speed up only visible prompt-writing or dictation actions from
  `<start evidence>` to `<end evidence>`. Keep their audible sound, preserve
  the original voice pitch, and make the retime begin and end on the action.
  Never speed up browsing, reading, waiting, or unrelated UI activity.
- Do not zoom the intro/title card. Add a zoom only when a clear visible
  target exists, such as an opened window, prompt box, relevant cursor action,
  or important UI. Center the target, use smooth ease-in/ease-out motion, and
  end before unrelated content begins.

Create the evidence-backed edit, retimed timeline, caption/effect rebases, and
Gate 1 review packet. Do not apply semantic changes or render a final candidate
until I approve Gate 1. If action boundaries or target identity are uncertain,
keep normal speed/no zoom and flag the item for review.
