# Security

Do not commit source media, `.env` files, API keys, signed URLs, model tokens,
private assets, or downloaded checkpoints. The project workspace and generated
evidence are intentionally ignored by Git.

If you find a security issue in the code or publishing workflow, do not include
private media or credentials in a public issue. Contact the repository
maintainer privately with a minimal reproduction and the affected commit.
